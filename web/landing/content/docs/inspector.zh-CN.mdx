---
title: 检查器
description: 浏览文件、带语法高亮地编辑、读 git 差异、搜索项目——在本机上，或在会话所在的那台机器上，都不用离开终端。
x-i18n:
  source_path: inspector.mdx
  source_hash: dcc8740e73f1a4ed2e31655fcbeddc027455acd96e81ea43040b2cbf69fea595
  generated_at: 2026-08-27
---

检查器是 Termio 用来处理终端*周边*一切的侧面板：文件、更改和搜索。它就在会话旁边，
让你不必切换应用也能看代码。

每个面板读的都是当前选中会话所在那台机器上的项目。指向一个跑在 VPS 上的会话，文件树、
搜索和差异就都是那台机器的——通过会话宿主读取，而不是把东西拷回本地。

## 文件

文件树映射工作目录。单击文件会在内置编辑器里打开，带语法高亮、软换行和实时的行列显示。
编辑自动保存——没有保存按钮，而且从另一台机器读出来的文件会写回那台机器。图片、PDF 和
HTML 则用「快速查看」预览。

### 编辑

这是一个真正的编辑器，不是查看器：

- **查找与替换**，用 Mac 的查找键：`⌘F`、`⌘G`、`⇧⌘G`、`⌘E`，以及替换和全部替换。
- **注释整行**，以及把一行或一段选区**上下移动**或**复制**。
- **Tab** 缩进整块，**回车**保持你当前所在的缩进，代码背后画有缩进参考线。
- 光标所在单词的其他出现处会被淡淡地刷上一层底色，不用搜索也能看出一个名字用在哪儿。

<DocsImage
  src="/screenshots/docs/06-files-editor.png"
  alt="Termio 文件检查器选中一个 Swift 文件，并在终端旁的语法高亮编辑器中打开"
  width={2424}
  height={1664}
/>

<DocsImage
  src="/screenshots/docs/23-quick-look.png"
  alt="Termio 文件检查器选中一个文件，并在只读的「快速查看」中打开其图片预览"
  width={2424}
  height={1664}
/>

## 更改

git 面板有三个只读标签：

- **更改** —— 当前的差异，以清晰的统一视图渲染。
- **对比** —— 当前分支相对它将要合入的分支：一个 PR 会带过去的文件和提交，从合并基点
  算差异，所以列表和平台上看到的一致。
- **历史** —— 最近的提交，带作者头像、可展开的文件列表和逐个提交的差异。

<DocsImage
  src="/screenshots/docs/07-changes-diff.png"
  alt="Termio「更改」标签选中 build-app.sh，并在终端旁显示红绿统一差异"
  width={2424}
  height={1664}
/>

<DocsImage
  src="/screenshots/docs/08-commit-history.png"
  alt="Termio「历史」标签同时显示一个提交、其中一个文件，以及该文件在这次提交中的差异"
  width={2424}
  height={1664}
/>

它是有意做成只读的：写入 git（提交、推送、开 PR）留在终端里，因为 Agent 和你自己的命令
本来就在那儿。

## 搜索 [#search]

检查器的 **搜索** 标签在整个项目里做内容搜索（底层是 `git grep`），并把你直接带到匹配的
那一行。它搜的是文件内部，不只是文件名。这就是你在编辑器里熟悉的「在文件中查找」。

结果以摘录呈现，并在匹配命中的位置着色，所以你不用挨个打开就知道要的是哪一条。搜索也
跟着会话走：另一台机器上的检出就在那台机器上搜，那个会话里的 `cd` 会把搜索范围一起带过去。

<DocsImage
  src="/screenshots/docs/09-project-search.png"
  alt="Termio 搜索检查器显示 DiffGapText 查询、按文件分组的结果和高亮的匹配文字"
  width={2424}
  height={1664}
/>

想按*名字*找文件？那是 [快速打开](/zh-CN/docs/navigation)（`⇧⌘O`），它同时也在会话和最近
的项目之间跳转。

## 从终端里打开文件

在终端输出里按住 Cmd 点击文件路径——即使在 Agent 的 TUI 里也行——Termio 会在编辑器里打开它。
Cmd 点击网址会用浏览器打开，`file://` 链接则打开一个只读预览浮层。

<DocsImage
  src="/screenshots/docs/22-open-terminal-path.png"
  alt="Termio 终端中的相对 Swift 文件路径在旁边的内置只读编辑器中打开"
  width={2424}
  height={1664}
/>

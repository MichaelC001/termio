---
title: 工作区
description: 工作区是侧栏的作用域——它装着项目和零散的终端，记得你离开时停在哪个会话上，并且只属于一台设备。
x-i18n:
  source_path: workspaces.mdx
  source_hash: 13032026bb64c2194c0a6429616a209b990ca732c6a7506d3e25ab34a2b51923
  generated_at: 2026-08-30
---

工作区是侧栏里一个具名的作用域。它装着项目和零散的终端；切到它，侧栏列出的内容就跟着
换——归在它下面的项目、worktree 和会话，别的都不列。

它是[设备](/zh-CN/docs/devices)和项目之间的那一层：每个工作区只属于一台机器，归在它
下面的一切都继承这一点。

## 切换 [#switching]

用 `⌘1` 到 `⌘9` 切换，也可以点侧栏工具栏里的切换器。`文件 ▸ 工作区` 里是同样的那几行。

数字是按位置算的：它跟着列表顺序走，所以列表一变，`⌘2` 指向的东西也就变了。这也是它们
不可重新绑定的原因——目标会漂移的绑定是留不住的。切换器会把数字画在名字旁边，你读它就
行，不用记。

工作区会记住你离开时停在哪个会话上，连同它的窗格、编组和检查器标签页。回到一个作用域，
回来的是你自己的现场，而不是排序最靠前的那一条。

如果一个工作区的名字本身没说清它在哪台机器上，切换器会把机器名写在旁边——所以切换工作
区也就是在机器之间移动。

## 管理工作区 [#managing-them]

**设置 ▸ 工作区**列出每个工作区、它所在的设备，以及里面有多少会话。重命名和移除就在它
们作用的那个工作区旁边，而不在切换器里——一个一次只勾选一行的菜单，只能为你当前所在的
那个工作区提供这两个动作。

移除一个工作区会关掉它的会话；磁盘上的文件夹不动。

新建工作区走 `文件 ▸ 工作区 ▸ 新建工作区…`，或者切换器里的同一行。当 Termio 知道不止一台
机器时，这一行会问新工作区放在哪台机器上。

<Callout type="note" title="一个工作区，一台设备">
  工作区不能跨两台设备。一份检出本来就只存在于某一台机器上，跨设备的工作区无非是把互相
  矛盾的叶子节点在视觉上摞在一起。既要动本机的仓库、又要动 VPS 上的仓库，那就是两个工
  作区。
</Callout>

<Cards>
  <Card href="/zh-CN/docs/devices" title="设备">
    添加机器、装好会话宿主，看看哪些能力跨得过去。
  </Card>
  <Card href="/zh-CN/docs/sidebar" title="侧栏">
    工作区框住的东西：项目、worktree，以及每个会话的状态。
  </Card>
</Cards>
